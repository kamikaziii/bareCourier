# =============================================================================
# bareCourier Environment Variables
# =============================================================================
# This file documents ALL environment variables needed for the application.
# Copy to .env and fill in your values.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
# Get these values from: Supabase Dashboard -> Project Settings -> API
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# -----------------------------------------------------------------------------
# Phase 2: Maps (Optional - features gracefully degrade without these)
# -----------------------------------------------------------------------------
# Mapbox - Get free token at https://mapbox.com
# Free tier: 50,000 requests/month
# Used for: Address autocomplete, route map display
PUBLIC_MAPBOX_TOKEN=pk.your-mapbox-token

# OpenRouteService - Get free key at https://openrouteservice.org
# Free tier: 2,000 requests/day
# Used for: Distance calculation between pickup/delivery
PUBLIC_OPENROUTESERVICE_KEY=your-openrouteservice-key

# -----------------------------------------------------------------------------
# Phase 2: Push Notifications (Optional - requires VAPID keys)
# -----------------------------------------------------------------------------
# Generate VAPID keys with: npx web-push generate-vapid-keys
# This outputs both public and private keys - put PUBLIC key here
PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key

# =============================================================================
# SUPABASE EDGE FUNCTION SECRETS
# =============================================================================
# These are NOT .env variables - they go in Supabase secrets!
# Set via CLI: supabase secrets set KEY=value
# Or via: Supabase Dashboard -> Edge Functions -> Secrets
#
# Required for Push Notifications:
#   supabase secrets set VAPID_PRIVATE_KEY=your-private-key
#   supabase secrets set VAPID_SUBJECT=mailto:your@email.com
#
# Required for Email Notifications:
#   supabase secrets set RESEND_API_KEY=re_xxx
#   supabase secrets set RESEND_FROM_EMAIL=noreply@yourdomain.com
#
# Note: RESEND_FROM_EMAIL must be from a verified domain in Resend,
# or use "noreply@resend.dev" for testing (limited to your own email).
# =============================================================================
